.. _ID:

ID
==

Identity operator: input fields are not modified. Typically used as a central
block in a SABER blockchain. This block requires no options other than its name:

.. code-block:: yaml

    saber central block:
        saber block name: ID
